{% extends 'base.html.twig' %}

{% block title %}Ajouter une dépense{% endblock %}

{% block body %}
    <h1>Ajouter une dépense pour le voyage : {{ trip.name }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Ajouter</button>
    {{ form_end(form) }}
    <a href="{{ path('app_expense_new', {trip_id: trip.id}) }}">Créer une nouvelle dépense</a><br>
{% endblock %}